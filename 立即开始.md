# 🚀 立即开始部署

你的GitHub仓库：**Haohaha-11/AllinOne**

## 📋 三步部署到互联网

### 第1步：推送代码（2分钟）

**⚠️ 重要：如果在国内，需要先配置代理！**

#### 1.1 配置Git代理（如果需要）

如果你有代理软件（Clash/V2Ray等）：

```bash
# 配置Git使用代理（将7890改为你的代理端口）
git config --global http.https://github.com.proxy http://127.0.0.1:7890
git config --global https.https://github.com.proxy http://127.0.0.1:7890
```

**详细配置方法：** 查看 `GIT_PROXY_SETUP.md`

#### 1.2 推送代码

打开终端，在项目目录执行：

```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

如果是第一次推送，执行：
```bash
git remote add origin https://github.com/Haohaha-11/AllinOne.git
git branch -M main
git push -u origin main
```

**完成后访问：** https://github.com/Haohaha-11/AllinOne

---

### 第2步：部署后端（2分钟）

1. **访问Railway**: https://railway.app
2. **登录GitHub账号**
3. **创建PostgreSQL数据库**:
   - 点击 "New Project"
   - 选择 "Provision PostgreSQL"
4. **部署后端**:
   - 点击 "New" → "GitHub Repo"
   - 选择 `Haohaha-11/AllinOne`
5. **配置**:
   - Settings → Root Directory: `backend`
   - Variables → 添加：
     ```
     NODE_ENV=production
     PORT=5000
     DATABASE_URL=${{Postgres.DATABASE_URL}}
     CORS_ORIGIN=*
     ```
6. **生成域名**:
   - Settings → Generate Domain
   - **记下URL**: `_______________________`

7. **运行迁移**（本地执行）:
   ```bash
   set DATABASE_URL=你的Railway数据库URL
   cd backend
   npm run db:migrate
   ```

---

### 第3步：部署前端（1分钟）

1. **访问Vercel**: https://vercel.com
2. **登录GitHub账号**
3. **导入项目**:
   - Add New → Project
   - 选择 `Haohaha-11/AllinOne`
4. **配置**:
   - Framework: Vite
   - Root Directory: `frontend`
   - Environment Variables:
     - Name: `VITE_API_URL`
     - Value: `你的Railway后端URL`
5. **部署**: 点击 Deploy

**完成后记下URL**: `_______________________`

---

### 第4步：更新CORS（30秒）

1. 回到Railway后端服务
2. Variables → 修改 `CORS_ORIGIN`
3. 改为你的Vercel URL
4. 保存

---

## ✅ 完成！

### 测试你的应用

**电脑上**: 打开你的Vercel URL
**手机上**: 在手机浏览器打开同样的URL

### 你的应用地址
- 前端: `https://你的项目名.vercel.app`
- 后端: `https://你的项目名.up.railway.app`

---

## 📚 详细文档

如果遇到问题，查看：

1. **[开始部署.md](开始部署.md)** - 详细步骤说明
2. **[GIT_COMMANDS.md](GIT_COMMANDS.md)** - Git命令参考
3. **[QUICK_DEPLOY.md](QUICK_DEPLOY.md)** - 完整部署指南
4. **[DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)** - 深入指南

---

## 💰 成本

- Vercel: **免费**
- Railway: **$5免费额度/月**
- 总计: **免费开始**

---

## 🎯 后续更新

修改代码后：
```bash
git add .
git commit -m "更新说明"
git push
```

自动部署，无需其他操作！

---

## ❓ 常见问题

### Q: 推送失败？

**如果提示连接失败：**
```bash
# 配置代理（将7890改为你的代理端口）
git config --global http.https://github.com.proxy http://127.0.0.1:7890
git config --global https.https://github.com.proxy http://127.0.0.1:7890
```

**详细解决方法：** 查看 `GIT_PROXY_SETUP.md`

**如果没有远程仓库：**
```bash
# 检查远程仓库
git remote -v

# 如果没有，添加：
git remote add origin https://github.com/Haohaha-11/AllinOne.git
```

### Q: 前端显示空白？
- 检查浏览器控制台（F12）
- 确认 `VITE_API_URL` 设置正确

### Q: 无法连接后端？
- 确认Railway后端正在运行
- 确认CORS_ORIGIN设置为Vercel URL

---

## 🎉 开始吧！

现在就执行第1步，推送代码到GitHub！

**祝部署顺利！** 🚀
