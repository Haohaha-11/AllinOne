# 🚨 紧急 - 立即操作

## ⚠️ 问题

Railway 还在从根目录构建，因为你**还没有设置 Root Directory**！

---

## 🎯 立即在 Railway Dashboard 操作

### 第1步：停止当前部署

1. 打开 Railway Dashboard
2. 找到正在失败的部署
3. 点击 **"Cancel"** 或等待失败

---

### 第2步：配置 Backend 的 Root Directory

#### 这是最关键的一步！

1. **点击 backend 服务卡片**
2. **点击 Settings 标签**
3. **向下滚动找到 "Root Directory"**
4. **输入**：
   ```
   backend
   ```
5. **点击保存或按回车**

---

### 第3步：配置 Frontend 的 Root Directory

1. **点击 frontend 服务卡片**
2. **点击 Settings 标签**
3. **向下滚动找到 "Root Directory"**
4. **输入**：
   ```
   frontend
   ```
5. **点击保存或按回车**

---

### 第4步：触发重新部署

#### Backend
1. 点击 backend 卡片
2. 点击 Deployments 标签
3. 点击右上角的 **"Deploy"** 按钮
4. 或者点击最新部署的 "..." → **"Redeploy"**

#### Frontend
1. 点击 frontend 卡片
2. 点击 Deployments 标签
3. 点击右上角的 **"Deploy"** 按钮

---

## 📊 设置 Root Directory 后会发生什么

### 之前（错误）
```
Railway 在根目录执行：
❌ npm run build --workspace=backend
❌ 找不到 workspace 配置
❌ 构建失败
```

### 之后（正确）
```
Railway 进入 backend 目录执行：
✅ npm ci
✅ npm run build
✅ npm start
✅ 构建成功
```

---

## 🔍 如何确认设置成功

### 查看 Backend Settings
1. 点击 backend 卡片
2. Settings 标签
3. 找到 "Root Directory"
4. 应该显示：`backend`

### 查看 Frontend Settings
1. 点击 frontend 卡片
2. Settings 标签
3. 找到 "Root Directory"
4. 应该显示：`frontend`

---

## 📋 Root Directory 设置位置

在 Railway Dashboard 中：

```
backend 服务
  └─ Settings 标签
      └─ Service Settings 部分
          └─ Root Directory 输入框
              └─ 输入: backend
```

```
frontend 服务
  └─ Settings 标签
      └─ Service Settings 部分
          └─ Root Directory 输入框
              └─ 输入: frontend
```

---

## ⚠️ 如果找不到 Root Directory 设置

### 可能的原因
1. Railway 界面更新了
2. 服务类型不对

### 解决方法
1. 在 Settings 中查找 "Source" 或 "Build" 相关设置
2. 或者删除服务，重新创建：
   - 点击服务卡片
   - Settings → 滚动到底部
   - "Delete Service"
   - 然后重新添加服务，选择正确的目录

---

## 🎯 重新部署后的预期日志

### Backend 日志应该显示
```
✓ Detected package.json in /app/backend
✓ Using Node.js 18.20.5
✓ Running npm ci
✓ Installing dependencies from package-lock.json
✓ axios@1.4.0
✓ cheerio@1.0.0-rc.10
✓ Running npm run build
✓ Compiling TypeScript
✓ Build successful
✓ Running npm start
✓ Server listening on port 5000
```

### Frontend 日志应该显示
```
✓ Detected package.json in /app/frontend
✓ Using Node.js 18.20.5
✓ Running npm ci
✓ Installing dependencies from package-lock.json
✓ Running npm run build
✓ Building with Vite
✓ Build successful
✓ Starting static server
```

---

## 💡 为什么必须设置 Root Directory

Railway 的 monorepo 支持需要你明确告诉它：
- **Backend 服务** → 在 `backend` 目录构建
- **Frontend 服务** → 在 `frontend` 目录构建

如果不设置，Railway 会：
- ❌ 在根目录查找 package.json
- ❌ 尝试使用 workspace 命令
- ❌ 构建失败

---

## 📱 设置完成后

1. ✅ Backend 会在 backend 目录构建
2. ✅ Frontend 会在 frontend 目录构建
3. ✅ 不再有 workspace 错误
4. ✅ 构建成功

---

## 🚀 下一步（设置 Root Directory 后）

1. **等待部署完成**（3-5分钟）
2. **添加环境变量**：
   - Backend: NODE_ENV, PORT, DATABASE_URL, CORS_ORIGIN
   - Frontend: VITE_API_URL
3. **生成域名**
4. **运行数据库迁移**
5. **测试应用**

---

**现在立即去 Railway Dashboard 设置 Root Directory！** 🚨

这是最关键的一步，不设置就无法构建成功！
